# Terminal Tools (Non-Interactive Mode)\n\n## terminalExec\n- **Non-interactive**: Direct `node-pty.spawn(prog, args)` per call (no persistent shell/UI).\n- **Output**: Full stdout/stderr (ANSI stripped) up to 500 lines preview + exitCode.\n- **Timeout**: 60s kill → `timedOut=true`, partial output.\n- **Shell cmds**: Prefix 'powershell -c' or 'bash -c \"cmd\"' for pipes/cd.\n- **Display**: Results in tool badge/chat (no panel).\n\n## Reusable\n`electron/services/ptySpawn.ts` for future user/explorer terminals.\n\n## Removed\n- Persistent PTY/agentPty/TerminalService/UI tabs.\n- Session tools (tail/search/commandOutput/restart).